.. highlight:: shell

============
Installation
============


Stable release
--------------

To install Human Design System Python Package, run this command in your terminal:

.. code-block:: console

    $ pip install hds

This is the preferred method to install Human Design System Python Package, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for Human Design System Python Package can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/flp9001/hds

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/flp9001/hds/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/flp9001/hds
.. _tarball: https://github.com/flp9001/hds/tarball/master
